{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "info",
    "description": "Info describing a file mirrored by Azul",
    "type": "object",
    "properties": {
        "content_type": {
            "type": "string",
            "description": "Content type of the mirrored file, as defined for the HTTP response header of the same name"
        },
        "schema": {
            "type": "string",
            "description": "URL of a JSON schema (like this one) describing the info object"
        }
    },
    "required": [
        "content_type",
        "schema"
    ]
}